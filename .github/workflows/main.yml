name: Android CI

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: gradle

      - name: Grant execute permission for gradlew
        run: chmod +x MessengerPro-1.1.3-jvtchgy/gradlew

      - name: Build debug APK
        working-directory: MessengerPro-1.1.3-jvtchgy
        run: ./gradlew assembleDebug --stacktrace
